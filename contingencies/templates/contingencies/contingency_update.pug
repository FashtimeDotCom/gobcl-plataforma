extends form.pug

- load i18n

block breadcrumbs
  li
    a(href="{% url 'contingency_list' %}")
      | {{opts.verbose_name_plural|capfirst}}
  li
    a(href="{{object.get_absolute_url}}")
      | {{object}}
