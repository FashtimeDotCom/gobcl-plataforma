extends form.pug

- load i18n
- load apphooks_config_tags

block breadcrumbs
  li
    a(href="{% namespace_url 'campaign_list' namespace='campaigns' default='' %}")
      | {{opts.verbose_name_plural.capfirst}}
