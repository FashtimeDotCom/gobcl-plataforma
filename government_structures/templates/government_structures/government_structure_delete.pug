extends base.pug
// TODO replace government_structure with the model name

- load i18n

block breadcrumbs
  li
    a(href="{% url 'government_structure_list' %}")
      | {{opts.verbose_name_plural|capfirst}}
  li
    a(href="{{object.get_absolute_url}}")
      | {{object}}


block content
  form(action="" method="post")
    {% csrf_token %}
    .alert.alert-danger
      {% blocktrans %}Are you sure you want to delete' "{{ object }}"?{% endblocktrans %}
    .text-right
      .btn-group
        input.btn.btn-primary(type="submit" value="{% trans 'Confirm' %}")
        a.btn.btn-default(href="{{object.get_absolute_url}}")
          | {% trans 'Cancel' %}
