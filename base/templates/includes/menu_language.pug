- load i18n menu_tags

for language in languages
  a.dropdown-item(
    href="{% page_language_url language.0 %}",
    title="{% trans 'Change to language:' %} {{ language.1 }}"
  )
    {{ language.0|upper }}